import styled from "styled-components";

export const StyledCryptoGraph = styled.div`
  height: 100%;
  background-color: ${(props) => props.theme.primaryColor};
  color: ${(props) => props.theme.accentColor};
`;
